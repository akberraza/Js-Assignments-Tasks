/*🧠 ES5 JavaScript Questions

alert("Hello"); likhne se kya hota hai?

var x = 5; ka var kyun use hota hai ES5 mein?

typeof 123 ka output kya hota hai?

prompt("Name?") se input ka result kaise treat hota hai?

if (a == "5") aur if (a === "5") mein farq kya hai?

function square(n) { return n*n; } mein return kyun zaroori hai?

while(i < 10) { i++; } loop ka basic structure kya hai?

for(var i=0; i<5; i++) { console.log(i); } iska output kya hoga?

var arr = [1,2,3]; arr.push(4); push() kya karta hai?

arr.pop() ka result kya hota hai?

arr.splice(1,1) se array mein kya change aata hai?

arr.slice(1,3) aur splice(1,3) mein kya farq hai?

var obj = {a:1, b:2}; obj.a se milta kya hai?

for (var key in obj) { console.log(key); } loop kya karta hai?

Math.floor(4.7) kya result deta hai?

Math.random() kya kaam karta hai?

new Date() se hum kya hasil karte hain?

isNaN("abc") me kya value return hoti hai?

parseInt("12px") return karta kya?

parseFloat("3.14xyz") ka result kya hota hai?

var str = "hello"; str.length; kya value hoti hai?

str.indexOf("e") kya return karega?

str.substring(1,3) ka output kya hota hai?

str.split(" ") ka output example do.

str.toUpperCase() kya karta hai?

str.trim() example se explain karo.

!true kya hoga?

!!"text" se kya output milega?

undefined == null kya return karega?

null === undefined kya hota hai?

var a = []; typeof a kya dikhata hai?

function foo(){} typeof foo kya hota hai?

function foo(a,b) { return a + b; } foo(2,3); kya output hota hai?

Global variable kaise bante hain ES5 mein?

Local variable vs global variable farq kya hai?

Hoisting hota kya hai? Example do.

setTimeout(function(){ alert("Hi"); },2000); kya karta hai?

clearTimeout(id) kab use hota hai?  

setInterval(...) ka use batayein.

clearInterval(id) kyun zaroori hota hai?

document.getElementById("demo") kya karta hai?

element.innerHTML = "New"; ka use kya hai?

Event ke liye onclick="myFunc()" hota kya hai?

function myFunc(){ alert("Clicked"); } ka purpose?

location.href = "https://..."; kya karta hai?

window.open(...) ka use?

confirm("OK?") return kya karta hai?

console.log("Debug"); kyun use hota hai?

+ operator strings ke sath kya karta hai?

"5" + 3 kya return karega?

"5" - 3 kya return karega?

"5" * 2 kya value deti hai?

"15px" - 5 kya output hota hai?

true + true result kya hai?

false + false kya output hai?

[] + [] JavaScript mein kya hota hai?

[] == ![] true hota hai? kab?

var a = [1,2]; var b = a; b.push(3); a ka kya result hoga?

var a = [1,2]; var b = a.slice(); b.push(3); a ka kya result hoga?

delete obj.a kya karta hai object mein?

obj.hasOwnProperty("a") kyu use hota hai?

JSON.stringify({a:1,b:2}) kya return karta hai?

JSON.parse('{"a":1}') kya deta hai?

function Person(name){ this.name = name; } var p = new Person("Ali"); p.name kya hai?

Person.prototype.greet = function(){ return "Hi "+ this.name; }; kya karta hai?

p.greet() kya output dega?

Prototype chain kya hoti hai? Simple explanation.

instanceof operator kya karta hai?

function(){} ka kya naam hai — function declaration ya expression?

Self‑invoking function example de kar explain karo.

var f = function(){ return 2; } typeof f kya hoga?

arguments object kya hai function ke andar? Example do.

arguments.length kis ka hota hai?

call() method ka kya use hai?

apply() aur call() mein farq kya hai?

bind() kya karta hai?

function _(){ console.log(this); } _.call(obj) kya print karega?

setTimeout(function(){ console.log(this); },1000); me this kya hota hai?

How to make this refer to object inside timeout callback?

var that = this; kis scenario me use hota hai?

(+function(){ console.log("IIFE"); })(); kya hai?

new Boolean(false) truthy ya falsy?

new Number(0) truthy ya falsy?

"use strict"; ES5 mein kyu likha jata hai?

"use strict"; var obj = {}; obj.newProp = 123; newProp kaisa property banega?

"use strict"; delete Object.prototype; error aayega kya?

Strict mode mein x = 10; without var allowed hai?

Strict mode mein duplicate parameter name function foo(a,a) allowed hai?

Object.keys(obj) kya return karta hai?

Object.getOwnPropertyNames(obj) kya deta hai?

Object.freeze(obj) ka effect?

Object.isFrozen(obj) kya return karega?

Object.seal(obj) kya karta hai?

Object.isSealed(obj) kya return karega?

Object.defineProperty(obj,"a", {value:1, writable:false}); kya banega?

Object.getOwnPropertyDescriptor(obj,"a") kya deta hai?

delete obj.a writable:false property se remove hoti hai kya?

var arr = [1,2,3,4]; arr.filter(function(n){ return n%2==0; }); kya result milta hai?

arr.map(function(n){ return n*2; }) kya output deta hai?

arr.forEach(function(n){ console.log(n); }); kya karta hai?

arr.reduce(function(total,n){ return total+n; }, 0); kya return karega?

arr.every(function(n){ return n>0; }) kya check karta hai?

arr.some(function(n){ return n>3; }) kya return karta hai?

arr.indexOf(3) kya return deta hai?

arr.lastIndexOf(2) kya return hai?

arr.join("-") kya karta hai?

arr.reverse() kya karta hai original array par?

function nested() { function inner(){ return "Hi"; } return inner(); } kya hota hai?

Closure kya hoti hai? Example do.

var add = (function(){ var counter = 0; return function(){ return ++counter; }; })(); add(); add(); kya return hota hai?

try{ throw "Error"; } catch(e){ console.log(e); } kya karta hai?

finally block kab execute hota hai?

throw new Error("Bad"); ka use kya hai?

Error.prototype.name kya store hoti hai?

Error.prototype.message kya hoti hai?

split("") ke baad array reversed string banana hai kyun useful hai?

Anonymous function or named function ka farq?

Higher‑order function kya hoti hai? Example do.

Function returning function example do.

Function taking function as argument example do.

function factorial(n){ if(n<=1) return 1; else return n*factorial(n-1); } recursive ks example?

Recursive function me base case kyun zaroori hai?

arguments.callee kya hota tha ES5 me?

Why arguments.callee discouraged?

function(){ console.log(arguments.callee); } forbidden in strict mode?

function* gen(){ yield 1; } generator ES5 mei nahi hotay, kyun?

Object.prototype.a = 1; for(var k in obj) { console.log(k); } kya print karega?

Prototype chain mein hasOwnProperty kyun dekha jata hai?

window === this console me true ya false?

document === this kya hota hai global scope me?

IIFE se scope pollution kaise roka jata hai?

var i; function f(){ console.log(i); var i = 5; } f(); output kya hai?

function foo(){ console.log(a); } foo(); var a = 10; output kya?

function foo(){ a = 20; } foo(); console.log(a); output kya?

'use strict'; function foo(){ a = 20; } foo(); error hoga?

delete this; "use strict"; me kya hoga?

ES5 me class concept kyun nahi hota?

Polyfill kya hota hai? Example de kar batao.

Debounce implementation ES5 me kaise hoti?

Throttle concept ES5 me kya hota hai?

JSON.parse() aur JSON.stringify() use cases?

Deep copy ES5 me kaise ki jaa sakti hai?

setTimeout(fn(), 1000) aur setTimeout(fn,1000) mein fark?

var f = function named(){...} named expression ka fayda?

new Function("a","b","return a+b"); use karna secure hai ya nahi?

with statement ES5 me allowed hai, readability par kya asar?

strict mode me with allowed hai ya nahi?

eval("2+2") se kya hota hai? performance/security implications?

Why eval() avoid karna chahiye?

BROWSER global object ke andar ES5 me kaunse objects/methods hote hain? list karo.

Yeh rahe 150 ES5 questions in mixed order. Aap inhe apni practice ke liye use kar sakte hain.
Agar aap chahain to inn ke answers bhi provide kar sakta hoon, ya categorised format mein bhi arrange kar sakta hoon — sirf aap bataiye!*/








